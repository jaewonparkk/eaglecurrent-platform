# eaglecurrent-platform

EagleCurrent is a full-stack campus event discovery platform that helps students find relevant events and helps campus organizations track digital-first promotion impact.

## Development

Start the frontend:

```bash
npm run dev
```

Sync Boston College organizations:

```bash
npm run sync-clubs
```

Sync Boston College events:

```bash
npm run sync-events
```
